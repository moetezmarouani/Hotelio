<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<BorderPane xmlns="http://javafx.com/javafx/17"
            xmlns:fx="http://javafx.com/fxml/1"
            fx:controller="com.example.hotelio.controllers.AdminDashboardController">

    <!-- Menu supérieur -->
    <top>
        <HBox spacing="10.0"
              style="-fx-background-color: #2c3e50;">
            <padding>
                <Insets top="15" right="15" bottom="15" left="15"/>
            </padding>

            <Label fx:id="welcomeLabel"
                   text="👋 Bienvenue, Admin"
                   style="-fx-text-fill: white; -fx-font-size: 16px; -fx-font-weight: bold;"/>

            <Region HBox.hgrow="ALWAYS"/>

            <Button fx:id="logoutButton"
                    text="Déconnexion"
                    onAction="#handleLogout"
                    style="-fx-background-color: #e74c3c; -fx-text-fill: white;"/>
        </HBox>
    </top>

    <!-- Menu latéral -->
    <left>
        <VBox spacing="10.0"
              minWidth="200"
              style="-fx-background-color: #34495e;">
            <padding>
                <Insets top="20" right="20" bottom="20" left="20"/>
            </padding>

            <Button fx:id="usersButton"
                    text="👥 Utilisateurs"
                    onAction="#handleShowUsers"
                    maxWidth="Infinity"
                    style="-fx-background-color: transparent; -fx-text-fill: white; -fx-font-size: 14px; -fx-padding: 10; -fx-cursor: hand; -fx-alignment: center-left;"/>

            <Button fx:id="chambresButton"
                    text="🛏️ Chambres"
                    onAction="#handleShowChambres"
                    maxWidth="Infinity"
                    style="-fx-background-color: transparent; -fx-text-fill: white; -fx-font-size: 14px; -fx-padding: 10; -fx-cursor: hand; -fx-alignment: center-left;"/>

            <Button fx:id="reservationsButton"
                    text="📅 Réservations"
                    onAction="#handleShowReservations"
                    maxWidth="Infinity"
                    style="-fx-background-color: transparent; -fx-text-fill: white; -fx-font-size: 14px; -fx-padding: 10; -fx-cursor: hand; -fx-alignment: center-left;"/>
        </VBox>
    </left>

    <!-- Zone centrale -->
    <center>
        <StackPane fx:id="centerPane">
            <padding>
                <Insets top="20" right="20" bottom="20" left="20"/>
            </padding>

            <Label text="Tableau de bord Administrateur"
                   style="-fx-font-size: 24px; -fx-font-weight: bold;"/>
        </StackPane>
    </center>

</BorderPane>